# Core AgentCore runtime wrapper — required for BedrockAgentCoreApp
bedrock-agentcore==0.1.4

# Strands Agents SDK — core + Bedrock provider (included by default)
strands-agents==0.1.6

# AWS SDK
boto3==1.34.144
